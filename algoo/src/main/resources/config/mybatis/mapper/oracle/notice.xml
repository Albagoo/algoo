<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="config.mybatis.mapper.oracle.notice">
	<insert id="insertNotice" parameterType="NoticeVO">
		<selectKey keyProperty="main_no" resultType="int"
		 order="BEFORE">
			select notice_seq.nextval from dual
		</selectKey>
		insert into notice(main_no, title, content,
	 		writer, read_count, regdate, authcode)
		values(#{mainNo},#{title},#{content},#{writer},
			#{readCount},#{regdate},#{authcode})
	</insert>
	
</mapper>